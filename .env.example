# Amp Bulk Executor – Example environment variables
# Copy to .env and fill in your values

# Required
AMP_API_KEY=your_amp_api_key
SOURCEGRAPH_API_URL=https://sourcegraph.com
SOURCEGRAPH_API_TOKEN=your_sourcegraph_access_token

# Optional
# If the Amp CLI is not on PATH, point directly to the binary
# AMP_BIN=/usr/local/bin/amp

# Only change this if you relocate the toolbox directory
# AMP_TOOLBOX=$PWD/src/toolbox
